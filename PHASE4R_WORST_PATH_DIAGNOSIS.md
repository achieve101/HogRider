# Phase 4R 最差路径因果诊断

> 本文档记录冻结 checkpoint 的因果诊断及其唯一预注册后续实验 E10-A。Oracle 使用
> held-out 数据，因此不可部署、不可用于训练；E10-A 未通过，正式 v3 模型与提交 ZIP
> 始终未改变。

## 结论

严格 LOPO Path 8 重放结果为 `17.2058280689 dB`，反弹为 `10.4783170692 dB`。
机械判据选择的根因是 **training_coverage_or_dictionary**，唯一冻结后续实验为
**E10-A / coverage_balanced_three_neighbor_synthesis**。E10-A 随后完成 seed 2026
八折严格 LOPO，但未通过预注册门槛，因此不进入三种子阶段。

## 声学 Oracle 层级（Path 8 独立评估集）

| 层级 | 主指标 (dB) | 反弹 (dB) |
|---|---:|---:|
| 最佳保留专家 | 12.986856 | 6.552050 |
| 静态 simplex | 16.552609 | 11.131566 |
| 静态 simplex + dictionary | 18.990100 | 10.539820 |
| 240 点块级 simplex + dictionary | 18.990101 | 10.539821 |
| 自由 2048-tap FIR | 10.312518 | 1.131607 |

自由 FIR 是三种固定初始化下的非凸最优已知解；未达门槛不能作为容量或记忆不足证明。容量判断以同架构已知 Path 8 三种子正对照为准。

## 对旧 Oracle 的修正

旧 `_oracle_latent_filter` 的 Path 8 结果为 `14.150668 dB`，但它所投影的旧 P3 Path 8 专家本身只有 `15.156740 dB`。因此该投影不能证明当前 FIR 或字典容量不足。

## 隔离与复现

- 8/8 折物理隔离：`True`。
- LOPO 最大主指标复现误差：`7.11e-15`。
- Path 9/10 未进入诊断训练输入：`True`。
- 完整逐场景、逐窗口、路由、覆盖相关性和哈希见 `artifacts/phase4r_worst_path_diagnosis.json`。
- 唯一预注册方案见 `artifacts/phase4r_preregistered_correction.json`。

## E10-A 改进方案

原训练采样器对 interpolate/extrapolate 使用确定性的单最近邻作为第二路径端点。E10-A
将候选范围扩大为三个 retained 邻居：分别在对齐 IR、50～8000 Hz 次级路径复响应和
主路径模板空间计算距离名次，以三种名次的平均值排序，然后均匀抽取三个最近邻之一。

- measured 与 augment 模式不改变端点选择；
- 路径混合比例、插值/外推幅度、候选遮蔽、模型、损失和训练超参数全部冻结；
- 每个 LOPO 折使用独立 retained-only 邻居表；
- held-out 路径及 Path 9/10 不得成为训练端点或模板来源。

该设计用于验证“单邻居路径合成覆盖不足”是否是 Path 8 退化的可修正上游原因，同时
避免把模型结构、记忆长度和训练覆盖一起改变。

## E10-A 八折结果

| Path | 冻结主增益 (dB) | E10-A 主增益 (dB) | 变化 (dB) | E10-A 反弹 (dB) |
|---:|---:|---:|---:|---:|
| 1 | 1.1602 | 1.1514 | -0.0087 | 2.6246 |
| 2 | 2.6055 | 2.7330 | +0.1275 | 5.1187 |
| 3 | 1.1473 | 0.6599 | -0.4873 | 2.8762 |
| 4 | -0.1347 | -0.2973 | -0.1625 | 6.8781 |
| 5 | 0.2235 | -0.3896 | -0.6131 | 9.6728 |
| 6 | 2.5421 | 2.5147 | -0.0273 | 5.2455 |
| 7 | 0.6751 | 0.6677 | -0.0074 | 0.0200 |
| 8 | -2.3413 | -1.7770 | +0.5643 | 10.5620 |

| 预注册门槛 | 要求 | 实际 | 结果 |
|---|---:|---:|---|
| Path 8 主增益 | ≥ -0.5 dB | -1.7770 dB | 失败 |
| Path 8 反弹 | ≤ 4.6475 dB | 10.5620 dB | 失败 |
| 最差覆盖折 Path 7/1/6 | 至少 2 折改善 | 0/3 | 失败 |
| 八折中位主增益 | ≥ 0.9112 dB | 0.6638 dB | 失败 |
| 非退化折 | ≥ 6/8 | 5/8 | 失败 |
| 物理隔离 | 8/8 | 8/8 | 通过 |

## 最终判断

E10-A 将 Path 8 的主增益改善了 `0.5643 dB`，说明训练覆盖确实解释了部分退化；但
Path 3/4/5 的主指标下降，最差覆盖三折也没有改善。均匀扩大邻居集合引入了不够局部的
合成方向，覆盖范围增加的同时降低了条件映射精度，因而不能冻结为正式改进。

本实验按规则停止：不运行 seeds 2027/2028，不执行正式十路径升级，不修改
`phase3g_submission_final_seed2027_v3`、权重、`dist/CCFANC.zip` 或技术报告。下一次
改进必须重新预注册，优先考虑更有选择性的覆盖或字典子空间修正，而不是直接扩大记忆。

完整 E10-A 汇总位于 `runs/phase4r_e10a_seed2026_lopo/lopo_summary.json`。
