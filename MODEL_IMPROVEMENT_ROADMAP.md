# DEEPANC 基线模型改进路线

## 1. 文档目的

本文档基于当前项目代码、README、Participant Kit v1.5 / 评分协议 v6、60 轮基线实测结果以及声学路径分析，给出后续模型改进的优先级与实验路线。

> **2026-08-10 状态修订**：Phase 3G `GenerativeInnovationFIRController` 已取代
> `TimeDomainANC` 成为正式模型。下文第 2～9 节保留原始问题分析和技术演化背景；
> 原先面向 TCN 的 Phase 4 已由第 10 节的 Phase 4R 取代。

> **2026-08-11 运行时修订**：Phase 4R 的 E09-A 等价优化已经完成。正式推理包升级为
> `phase3g_submission_final_seed2027_v3`；运行时加固通过，最差路径诊断仍留待后续。

推荐的总体方向是：

> 先让训练和验证严格对齐正式评分，再进行路径鲁棒训练，最终升级为带误差反馈的轻量流式自适应模型。

不建议仅通过增加 epoch、网络层数或通道数来提升模型。当前主要瓶颈不是模型容量不足，而是训练目标、数据采样、路径泛化、在线适应能力和正式部署接口没有对齐。

---

## 2. 当前基线与实测结果

### 2.1 模型结构

当前 `TimeDomainANC` 是纯时域因果 TCN：

- 输入：参考噪声 `x(t)`；
- 输出：扬声器控制信号 `y(t)`；
- 隐藏通道：32；
- 残差空洞卷积块：10 层；
- dilation：`1, 2, 4, ..., 512`；
- 参数量：约 42,764；
- 感受野：2047 samples，48 kHz 下约 42.6 ms；
- 计算量：约 42k MAC/sample，即约 2.0 GMac/s。

网络输出经过次级路径 `S(z)` 后得到实际反噪声：

```text
a(t) = y(t) * S(z)
e(t) = d(t) - a(t)
```

当前训练目标为整段时域残余噪声 MSE：

```python
loss = mean(e(t) ** 2)
```

### 2.2 60 轮实测结果

| 测试条件 | 20 轮 | 60 轮 | 60 轮 README 旧版参考 |
|---|---:|---:|---:|
| 未见噪声 + 已见路径 | 2.57 dB | **5.17 dB** | 5.53 dB |
| 未见噪声 + 未见路径 | 2.37 dB | **3.48 dB** | 3.93 dB |

60 轮各路径结果：

```text
已见路径 1～8：6.81, 8.21, 7.14, 4.70, 4.43, 4.97, 0.35, 4.74 dB
未见路径 9～10：5.79, 1.18 dB
```

模型已经基本复现旧版官方基线，但路径间表现非常不均衡。

### 2.3 路径分析

`sh.npy` 原始形状为 `(1967, 10)`，转置后为 10 条长度 1967 的次级路径，每条约 41 ms。

关键发现：

- 路径 7 在 50 Hz～5 kHz 内存在约 `-19.65 dB` 的深陷波，对应 NR 仅 `0.35 dB`；
- 未见路径 9 与训练路径 2 的时域绝对相关性约 `0.94`，因此仍取得 `5.79 dB`；
- 未见路径 10 与最接近训练路径的相关性约 `0.80`，频响距离更大，对应 NR 仅 `1.18 dB`；
- 当前模型对深陷波、相位差异、低增益和分布外路径的鲁棒性不足。

### 2.4 频域现象

当前结果图显示：

- 低频通常能够有效下降；
- 约 2～10 kHz 的部分频段出现残余噪声高于原始噪声；
- 部分路径虽然整段能量 NR 为正，但存在明显高频反弹；
- 正式评分会对 1～8 kHz 内每个窗口最严重的反弹进行处罚。

因此，当前整段能量 NR 不能完整代表正式成绩。

---

## 3. 正式评分约束

Participant Kit v1.5 / 协议 v6 的单场景时序为：

```text
0.5 秒初始化 + 3.0 秒正式计分 = 3.5 秒连续闭环
```

要求：

- 初始化与计分之间不调用 `reset()`；
- 模型状态和次级路径状态连续；
- 3 秒计分段划分为六个连续、互不重叠的 0.5 秒窗口；
- 每个窗口独立计算 1/3 倍频带指标；
- 六个窗口等权平均；
- 正式测试的多个场景再次等权平均。

主要指标：

1. **50 Hz～5 kHz 未加权 1/3 倍频带平均降噪量**：越高越好；
2. **1 kHz～8 kHz 平均窗口最坏频带反弹**：越低越好；
3. **模型参数量**；
4. **单个采样事件峰值 MAC**，包括初始化阶段的一次性计算。

正式接口要求模型严格逐采样工作：

```python
reset()
process_sample(...)
get_complexity()
```

前馈模型在时刻 `t` 接收 `x[t]`，反馈模型允许接收 `x[t]` 和 `e[t-1]`。

---

## 4. 当前基线的主要问题

### 4.1 训练目标与评分不一致

时域 MSE 会优先优化高能量频段，而正式指标对每个有效 1/3 倍频带等权。MSE 下降不保证正式主指标提高，也不会主动限制高频反弹。

### 4.2 每轮训练更新次数过少

当前数据集长度等于训练路径数量：

```python
def __len__(self):
    return len(self.path_indices)
```

训练路径为 8，batch size 也是 8，因此每个 epoch 通常只有一次参数更新。当前 60 epoch 实际约等于 60 次 `optimizer.step()`。

### 4.3 数据没有被完整使用

`餐厅.WAV` 使用大写扩展名，其目标文件采用 `餐厅.WAV_scene_XX.wav` 命名。现有扫描逻辑只接受小写 `.wav`，所以这类数据被忽略。

### 4.4 验证集过小且划分固定

当前只使用路径 9、10 作为未见路径测试，结果容易受特定路径影响，也容易在反复调参后间接过拟合这两条路径。

### 4.5 感受野与次级路径长度接近

模型感受野约 42.6 ms，次级路径约 41 ms，留给低频周期、初级路径延迟、混响尾部和在线适应的上下文不足。

### 4.6 纯前馈模型缺乏在线路径辨识能力

理想控制器取决于初级路径和次级路径。模型只观察 `x[t]` 时，很难判断当前佩戴位置和物理路径。上一采样点残差 `e[t-1]` 能提供当前抵消方向是否正确的反馈，是提升未见路径性能的重要信息。

### 4.7 当前网络尚未实现正式流式接口

网络虽然是因果结构，但 `forward()` 一次处理完整波形。正式提交需要维护卷积缓存或递归状态，每个采样点只计算一次新增状态，不能重复计算全部历史。

---

## 5. 改进路线总览

| 阶段 | 目标 | 主要改动 | 优先级 |
|---|---|---|---:|
| Phase 0 | 建立可信实验基础 | 随机种子、日志、checkpoint、完整数据扫描 | P0 |
| Phase 1 | 对齐正式评分 | v6 窗口评测、1/3 倍频带损失、反弹惩罚 | P0 |
| Phase 2 | 提升路径鲁棒性 | 路径增强、最坏路径优化、交叉验证 | P1 |
| Phase 3 | 引入在线适应 | `e[t-1]` 反馈、小型 GRU/控制器专家库 | P1 |
| Phase 4R | 运行时加固与最差路径诊断 | 峰值计算摊平、RTF 余量、路径 8 因果诊断、条件式记忆消融 | P1 |
| Phase 5 | 系统调优 | 消融实验、超参数搜索、正式提交验证 | P2 |

---

## 6. Phase 0：建立可信的强基线

### 6.1 修正训练采样

建议把 dataset 的长度改为可配置的虚拟样本数，例如：

```text
samples_per_epoch = 500～2000
batch_size = 4～8
```

每个样本再随机选择：

- 噪声类型；
- 次级路径；
- 音频起点；
- 增益与路径增强参数。

训练时应均衡采样 noise/path 组合，避免某些路径长期得不到训练。

### 6.2 完整识别数据文件

扫描扩展名时统一使用：

```python
Path(file).suffix.lower() == ".wav"
```

同时建立显式的噪声名到目标文件名映射，正确处理 `餐厅.WAV_scene_XX.wav`，不要直接依赖简单字符串拼接。

### 6.3 固定随机性

至少固定：

- Python `random`；
- NumPy；
- PyTorch CPU；
- PyTorch CUDA。

每个关键实验建议使用 3 个随机种子，汇报均值和标准差。

### 6.4 保存模型与训练状态

建议保存：

```text
checkpoints/latest.pt
checkpoints/best_primary_score.pt
checkpoints/best_robust_score.pt
```

checkpoint 至少包含：

- `model.state_dict()`；
- optimizer state；
- epoch / global step；
- 模型配置；
- 数据划分；
- 随机种子；
- 验证指标。

### 6.5 建立日志

每次验证记录：

- 平均正式主指标；
- 高频反弹；
- 每个 0.5 秒窗口得分；
- 每条路径得分；
- 最差路径得分；
- 控制信号峰值、RMS；
- NaN、Inf 和饱和次数；
- 参数量、MAC 和推理速度。

---

## 7. Phase 1：训练目标对齐正式评分

建议使用组合损失：

```text
L = L_time
  + λ_band · L_third_octave
  + λ_rebound · L_rebound
  + λ_control · L_control
  + λ_robust · L_worst_path
```

### 7.1 时域稳定项 `L_time`

保留残差时域 MSE：

```text
L_time = mean(e²)
```

作用是保持整体波形稳定，避免仅优化频域指标导致异常瞬态。

### 7.2 1/3 倍频带损失 `L_third_octave`

使用与正式评分一致的窗口和频带划分，最小化 50 Hz～5 kHz 内各频带的对数残余能量比：

```text
L_third_octave = mean_band(log(P_error + eps) - log(P_target + eps))
```

各频带应等权，而不是按绝对能量加权。

训练时可先使用 FFT 频带掩码近似 1/3 倍频带功率，验证时必须调用正式实现核对。

### 7.3 高频反弹损失 `L_rebound`

对 1 kHz～8 kHz 内残余功率大于原始功率的频带加惩罚：

```text
rebound_b = ReLU(log(P_error + eps) - log(P_target + eps))
```

再使用最大值或平滑最大值逼近每个窗口的最坏频带：

```text
L_rebound = mean_window(softmax_max(rebound_b))
```

### 7.4 控制输出正则 `L_control`

建议同时约束：

- 控制信号 RMS；
- 峰值幅度；
- 高频变化率；
- 超过安全阈值的部分。

例如：

```text
L_control = mean(y²) + α · mean(ReLU(abs(y) - y_limit)²)
```

这可以降低强逆滤波、扬声器饱和和高频不稳定风险。

### 7.5 最坏路径损失 `L_worst_path`

不能只优化 batch 平均值。可选择：

- batch 最大损失；
- top-k 平均；
- CVaR，即最差 20%～30% 样本的平均损失。

推荐：

```text
L_worst_path = mean(top_25_percent(path_losses))
```

它将迫使模型改善路径 7、路径 10，而不是继续提高已经表现很好的路径。

### 7.6 正式时序训练

训练样本应逐步接近正式的 3.5 秒时序：

```text
0.5 秒初始化 + 6 × 0.5 秒计分窗口
```

考虑到 RTX 3050 Ti 显存，建议使用连续状态的分块训练：

- 每次处理 0.25～0.5 秒；
- 块之间保持模型状态；
- 使用 truncated BPTT；
- 在初始化与计分边界处不调用 `reset()`；
- 在场景之间才调用 `reset()`。

---

## 8. Phase 2：路径鲁棒训练

### 8.1 物理合理的次级路径增强

在训练时对路径进行随机增强：

- 增益变化：建议从 ±3 dB 开始；
- 整数或小数采样延迟变化；
- 平滑频响倾斜；
- 轻微时间尺度变化；
- 相近 IR 的插值；
- 低幅度混响尾部扰动；
- 局部平滑频响衰减。

不建议直接对每个 IR tap 加入较强独立白噪声，这可能破坏真实物理结构。

### 8.2 多路径联合训练

对同一个参考噪声片段同时选择多条路径，分别计算闭环残差：

```text
L_batch = mean_path(L_path) + β · worst_path(L_path)
```

这样可以直接训练一个对路径变化更稳健的控制器。

### 8.3 陷波保护

路径 7 表明次级路径深陷波是明显风险。在路径幅频响应很低的频带，不应进行不受约束的强逆滤波。

可采用：

- 控制输出正则；
- 基于路径增益的训练频带权重上限；
- 对陷波频带使用鲁棒而非精确逆滤波目标；
- 在路径增强中加入陷波位置变化；
- 高频输出限幅或平滑。

### 8.4 交叉验证

建议建立以下验证：

1. Leave-One-Path-Out；
2. Leave-One-Noise-Out；
3. Noise + Path 双留出；
4. 每种划分运行至少 3 个随机种子。

模型选择时重点看：

```text
平均主指标
最差路径主指标
高频反弹
首个计分窗口
不同随机种子的标准差
```

---

## 9. Phase 3：反馈式在线自适应模型

### 9.1 为什么需要反馈

纯前馈模型只接收 `x[t]`，不能直接观察当前控制结果是否正确。正式接口允许反馈模型接收上一采样点残差：

```python
process_sample(reference_sample, previous_error_sample)
```

`e[t-1]` 包含路径失配、相位错误和输出过强的信息，可以用于在线路径识别和自适应。

### 9.2 推荐总体结构

```text
x[t] ──> 前馈多尺度控制器 ──────────────┐
                                         ├──> y[t]
e[t-1] -> 小型状态估计器 -> 调制/门控 ──┘
```

其中：

- 前馈分支快速生成基础反噪声；
- 反馈分支根据过去残差估计当前路径和控制误差；
- 反馈状态调节输出增益、卷积通道或专家权重；
- 0.5 秒初始化阶段用于快速收敛；
- 初始化和计分阶段保持状态连续。

### 9.3 首选方案：线性控制器专家库 + 小型 GRU

推荐最终模型：

```text
4～8 个因果 FIR/线性卷积控制器
+ 小型 GRU 路径状态估计器
+ 专家混合门控
+ 安全输出限制器
```

GRU 可以接收：

- 当前 `x[t]`；
- 上一残差 `e[t-1]`；
- 上一控制输出 `y[t-1]`；
- 短期能量或滤波统计量。

GRU 输出专家权重或 FiLM 调制参数，最终控制信号为多个线性控制器输出的加权和。

优势：

- ANC 主要物理过程接近线性，线性专家更稳定；
- 不同专家可以覆盖不同路径类型；
- 非线性只负责路径识别和控制器选择，不直接制造复杂音频波形；
- 更容易抑制高频谐波和反弹；
- 参数量和 MAC 容易核算；
- 便于逐采样流式实现。

### 9.4 备选方案：反馈调制的流式 TCN

保留现有 TCN 主体，增加一个小型 GRU 或递归状态网络，根据 `e[t-1]` 产生：

- 通道增益；
- 输出增益；
- 残差块 FiLM 参数；
- mixture-of-experts 权重。

该方案改动较平滑，但稳定性和计算量需要重点检查。

### 9.5 稳定性措施

- 对反馈状态做范数限制；
- 输出使用软限幅而非硬裁剪；
- 训练中加入随机路径切换和突发噪声；
- 训练中覆盖初始化后的第一个窗口；
- 对 NaN、Inf、异常峰值设置强惩罚；
- `reset()` 必须清空所有状态；
- 同一输入在两次 `reset()` 后必须可复现。

---

## 10. Phase 4R：运行时加固与最差路径诊断

### 10.1 原 Phase 4 的适用性修订

原 Phase 4 面向 32 通道、10 层空洞卷积的 `TimeDomainANC`，提出深度可分离卷积和
扩大 TCN 感受野。当前正式模型已切换为 Phase 3G 的 FIR 专家、解析创新路由和低秩
生成残差，不再包含 TCN 卷积堆栈，因此以下项目取消：

- 不再把普通卷积替换为深度可分离卷积；
- 不再增加 TCN dilation、层数或通道数；
- 不再以 TCN 感受野作为扩大记忆的主要手段。

原 Phase 4 的流式缓存目标已经完成：当前实现维护 FIR 环形缓存、候选次级路径
overlap-save 历史、创新频谱窗口、EWMA、GRU hidden、解析路由权重和 latent 激活，
不会在每个采样点重新推理完整历史。

### 10.2 当前记忆与复杂度基线

Phase 3G 已具有多时间尺度记忆：

- 2048-tap 控制 FIR：约 42.7 ms 控制历史；
- 4096 点创新窗口：约 85.3 ms 路径证据；
- GRU 隐状态：在完整 3.5 秒录音内连续保留；
- 240 点更新周期：约 5 ms 的路由和 FIR 激活更新粒度。

当前正式模型为 41,552 参数，平均约 12,392 MAC/sample，相比 P1-E2 的
42,048 MAC/sample 已降低约 70.5%。Participant Kit validate CPU RTF 为 0.695，
完整公开闭环 RTF 为 0.959。平均复杂度已经达标，但公开完整闭环距离 RTF=1 的硬边界
余量较小，且 240 点块边界的峰值事件约为 2,484,720 MAC，因此下一步重点应是运行时
加固，而不是继续压缩一个已经较小的网络参数量。

### 10.3 运行时加固

先对正式提交包做分项 profile，至少分别记录：

- 八个候选次级路径卷积；
- 4096 点创新频谱和 `J_i` 计算；
- 八个专家 FIR 与 16 个字典原子的输出；
- 52 维特征、GRU 和有效 FIR 合成；
- Python/NumPy/PyTorch 接口与逐采样调度开销。

同时报告整段 RTF 和块边界耗时的 P50/P95/P99/最大值。优化优先级为：

1. 复用公共 FFT、历史窗口和中间频谱，避免候选间重复计算；
2. 在不改变因果时序和数值结果的前提下向量化候选路径与 FIR bank；
3. 将可安全拆分的块边界计算摊到后续 240 个采样事件；
4. 减少 Python 对象创建、张量转换和小算子调度；
5. 在 Participant Kit 支持的依赖范围内验证不同 CPU 的稳定余量。

建议把完整公开闭环 RTF 的工程目标从“刚好不超过 1”收紧为 `≤0.9`，并要求
优化前后逐点输出误差不超过 `1e-6`、官方声学指标不退化、`state_dict` 哈希不变。

E09-A 的 profile 证实，候选路径和路由 FFT 已复用公共频谱并批量计算，当前首要浪费
不是重复 FFT，而是提交包装器在每个采样点重复进入 `torch.inference_mode()`。移除该
冗余上下文后，Ryzen 7 5800H 上的正式实测为：

- v2 基线三次完整闭环 RTF：P50 `0.9427`，最大 `0.9620`；
- v3 候选十次完整闭环 RTF：P50 `0.6760`，P95 `0.7000`，最大 `0.7082`；
- 240 点块边界 P99：`1.7537 ms` 降至 `1.6023 ms`，候选最大 `2.2348 ms`；
- 168,000 点输出和闭环残差逐点完全一致，最大绝对误差均为 `0`；
- 权重文件、`state_dict`、参数量、峰值 MAC 和官方声学指标均不变。

因此按“稳定达到 `RTF≤0.8` 即停止”的规则保留 E09-A，不执行缓存状态机重写 E09-B。
把块边界计算摊到后续采样会延迟有效 FIR 更新，与逐点等价要求冲突，本轮不采用。

### 10.4 最差路径诊断优先于扩大记忆

Phase 3G 严格 LOPO 中位增益为 `+0.9112 dB`、6/8 折不退化，但留出路径 8 仍相对
P1-E2 退化 `2.3413 dB`。现有证据不能把该问题归因于记忆不足：2048-tap oracle 专家
已经通过已测路径容量门槛，解析路由也已基本解决路径识别。因此在改变结构前必须区分：

- 创新模板是否对路径 8 或其邻域产生错误排序；
- 路径 8 是否超出当前插值/外推训练分布；
- 16 维残差字典是否无法表示相应最优控制核；
- 候选遮蔽是否没有覆盖该外推方向；
- 路由正确但 latent 生成是否不足，或路由本身仍不稳定。

诊断必须继续遵守严格 LOPO：不得使用路径 8 的 disturbance、次级路径、专家、模板、
字典初始化或合成端点训练候选模型。

### 10.5 条件式记忆消融

只有诊断显示性能随历史长度单调改善时，才允许增加记忆。先做不改变其他因素的固定消融：

| 消融项 | 固定候选 | 观察指标 |
|---|---|---|
| 创新窗口 | 2048 / 4096 / 8192 点 | LOPO 路径 8、切换恢复、RTF、峰值耗时 |
| FIR 长度 | 1024 / 2048 / 4096 taps | oracle latent、LOPO、参数/缓存/MAC |
| GRU 状态 | 正常连续 / 定期清零诊断 | 首窗、后续窗口、路径切换 |
| 更新周期 | 120 / 240 / 480 点 | 恢复时间、峰值事件、平均 RTF |

不得同时搜索窗口、FIR、hidden 和损失权重。如果更长记忆没有明确改善严格留出路径，
维持当前 2048-tap、4096 点窗口和 hidden 32 配置。

### 10.6 Phase 4R 停止与验收条件

Phase 4R 不以参数更少或单机一次计时更快作为成功。候选必须同时满足：

- 十路径主指标、反弹和综合分不劣于当前 seed 2027 正式模型的允许数值误差；
- 严格 LOPO 中位增益不低于 `+0.9112 dB`，且至少 6/8 折不退化；
- 路径 8 相对 P1-E2 的退化显著收窄；若未改善，不得以平均分替代该结论；
- Participant Kit validate 与完整公开闭环 RTF 均不超过 0.8；
- 块边界 P99 和最大耗时有明确报告，不出现非有限状态或输出；
- 三个种子的 reset、causality、finite、peak 和 `state_dict` 不可变检查全部通过。

若只能优化速度而不能改善路径 8，可把结果记录为“部署加固通过、泛化未改善”；若速度
或声学指标任一退化，则保留当前 Phase 3G 正式包。

当前状态为“部署加固通过、泛化未评估”：E09-A 对模型输出严格等价，因此既不改善也不
损害现有 LOPO 结论；路径 8 因果诊断将在独立后续阶段执行。

---

## 11. 实验与消融顺序

建议严格按以下顺序推进：

1. 修正 DataLoader，使每轮包含足够随机片段；
2. 完整纳入 8 类噪声；
3. 固定随机种子；
4. 增加训练日志和 checkpoint；
5. 接入正式 v6 窗口化评测；
6. 加入 1/3 倍频带损失；
7. 加入高频反弹损失；
8. 加入控制输出约束；
9. 加入最差路径/CVaR 损失；
10. 加入路径增益、延迟和频响增强；
11. 执行 Leave-One-Path-Out 验证；
12. 增加 `e[t-1]` 反馈并实现 FIR 专家、创新路由和生成式残差；
13. 验证显式流式缓存、因果性、reset 和状态不可变；
14. 完成严格 LOPO、三种子和封存路径最终评估；
15. 对正式 Phase 3G 运行时做分项 profile，并降低块边界峰值和 RTF；
16. 对 LOPO 路径 8 做模板、训练分布、字典和 latent 的因果诊断；
17. 只有诊断支持时才进行单因素记忆消融，不再搜索 TCN 深度可分离卷积。

每次实验只改变一个主要因素，必须保留对应的 baseline 和配置文件。

---

## 12. 建议的实验表

| 实验编号 | 模型/训练改动 | 目的 | 核心对比指标 |
|---|---|---|---|
| E00 | 当前 60 轮基线 | 对照 | 平均 NR、最差路径 |
| E01 | 增加 steps/epoch | 验证训练不足 | v6 主指标 |
| E02 | E01 + 频带损失 | 对齐主评分 | 50 Hz～5 kHz 指标 |
| E03 | E02 + 反弹损失 | 抑制高频放大 | 1～8 kHz 反弹 |
| E04 | E03 + CVaR | 改善坏路径 | 最差路径、P10/P25 |
| E05 | E04 + 路径增强 | 提升未见路径泛化 | Leave-One-Path-Out |
| E06 | FIR 专家 + 反馈路由 | 验证线性控制容量和路径适应 | oracle、路由准确率、首窗 |
| E07 | 创新误差解析路由 | 修复反馈统计辨识失败 | 路由、切换恢复、LOPO |
| E08 | 创新条件生成式 FIR | 跳出专家凸包 | 严格 LOPO、压力集、十路径 |
| E09 | Phase 3G 运行时 profile 与等价优化 | 增加部署余量 | RTF、P99/峰值、逐点误差 |
| E10 | 路径 8 因果诊断 | 定位剩余泛化失败 | 创新排序、oracle latent、遮蔽覆盖 |
| E11 | 条件触发的单因素记忆消融 | 验证是否确有记忆瓶颈 | 路径 8、切换、RTF、峰值 MAC |

---

## 13. 模型选择建议

不应只使用平均 NR 选择最佳模型。建议定义一个内部稳健指标：

```text
robust_score = mean_primary_score
             - α × rebound_score
             + β × worst_path_primary_score
             + γ × first_window_score
```

其中：

- `mean_primary_score`：正式主指标平均值；
- `rebound_score`：正式高频反弹，越低越好；
- `worst_path_primary_score`：最差路径主指标；
- `first_window_score`：初始化后第一个 0.5 秒窗口得分。

具体权重先用于内部模型选择，最终仍需分别汇报官方指标，不能只汇报合成分数。

---

## 14. 不建议优先进行的改动

- 单纯把 epoch 从 60 增加到几百；
- 单纯把 hidden channels 从 32 增加到 64；
- 只根据训练 Loss 或整段能量 NR 选模型；
- 只在路径 9、10 上反复调参；
- 在次级路径陷波频带做不受约束的强逆滤波；
- 使用大尺寸 Transformer 而忽略逐采样接口；
- 使用大 STFT 帧造成不可接受的算法延迟；
- 在 `process_sample()` 中重复推理完整历史；
- 引入正式推理环境不支持的第三方依赖；
- 只优化平均性能而不记录最差窗口和最差路径。

---

## 15. 阶段性验收标准

### Milestone A：可信训练基线

- 8 类噪声全部被使用；
- 每轮不再只有一次参数更新；
- 实验可由固定种子复现；
- 自动保存最佳模型；
- 有每路径、每窗口指标日志。

### Milestone B：评分对齐

- 本地验证严格使用 0.5 s + 3.0 s 时序；
- 能输出六窗口 1/3 倍频带指标；
- 能计算 1～8 kHz 反弹；
- checkpoint 按正式主指标选择。

### Milestone C：路径鲁棒性

- Leave-One-Path-Out 平均性能提高；
- 最差路径不再接近 0 dB；
- 路径增强后高频反弹没有恶化；
- 多随机种子方差下降。

### Milestone D：反馈式流模型

- 正确实现 `reset()`；
- 严格使用 `x[t]` 和 `e[t-1]`；
- 首个窗口快速收敛；
- 通过 `validate_submission.py`；
- 通过 `run_public_demo.py`；
- 参数量和峰值 MAC 声明一致；
- 实时因子满足要求。

Phase 3G 已满足 Milestone D，并完成严格 LOPO、三种子和十路径正式升级。

### Milestone E：Phase 4R 运行时加固与尾部风险

- 完整公开闭环 RTF 在目标 CPU 上稳定不超过 0.8；
- 分项报告平均、P95、P99 和最大块边界耗时；
- 等价优化前后逐点输出误差不超过 `1e-6`，官方声学指标不退化；
- 明确路径 8 退化来自路由、模板、训练分布还是字典表达；
- 只有实验证明更长历史有效时才扩大窗口或 FIR；
- 三种子的状态不可变、reset、causality、finite 和 peak 检查继续通过。

---

## 16. 最终推荐模型

当前正式模型已经确定为：

> Phase 3G `GenerativeInnovationFIRController`：八个冻结的 2048-tap FIR 专家提供
> 稳定基底，解析创新误差路由根据 `x[t]` 和 `e[t-1]` 形成专家权重，小型冻结 GRU
> 生成 16 维低秩 FIR 残差激活；所有训练参数在部署期间保持不变。

当前不再建议恢复 TCN 或以扩大感受野作为默认下一步。后续优先级为：

1. 降低完整公开闭环 RTF 和 240 点块边界峰值，增加低性能 CPU 的部署余量；
2. 对严格 LOPO 路径 8 做因果诊断，优先改善真实的尾部泛化风险；
3. 只在诊断证明历史长度不足时进行单因素记忆扩展；
4. 任何候选都必须重新通过官方六窗口评分、严格 LOPO、三种子和状态不可变检查。

当前正式 checkpoint、最终指标和剩余风险见 `artifacts/phase3g_formal_model.json`、
`PHASE3G_GENERATIVE_FIR.md` 与 `FORMAL_MODEL_IMPROVEMENT_REFERENCE_MAP.md`。

---

## 17. 相关项目文件

- `legacy_models/phase0_phase1/model.py`：当时的因果 TCN；
- `dataset.py`：数据加载与次级路径物理卷积；
- `legacy_models/phase0_phase1/train.py`：Phase 0 训练与旧版整段 NR 评估；
- `legacy_models/phase0_phase1/evaluate_custom_scenes.py`：旧版离线声学分析；
- `phase3g_model.py`：当前正式生成式 FIR 控制器；
- `phase3g_submission_runtime.py`：当前逐采样运行时；
- `PHASE3G_GENERATIVE_FIR.md`：Phase 3G 设计、实验和正式升级结果；
- `LEGACY_MODEL_FILES.md`：旧模型文件位置与用途；
- `README.md`：赛题说明、基线结果和评分公告；
- `EvaluationMetrics.zip`：旧版声学指标工具；
- `DEEPANC_PARTICIPANT_KIT.zip`：Participant Kit v1.5、正式流式接口和协议 v6 工具。
